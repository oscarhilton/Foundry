{
  "buildCommand": "turbo run build --filter=@foundry/simulator --force && rm -rf dist && cp -R apps/simulator/dist dist",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": null
}
